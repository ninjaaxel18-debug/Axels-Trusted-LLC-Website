const menu=document.querySelector('.menu'),nav=document.querySelector('.nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const range=document.querySelector('.ba input'),before=document.querySelector('.before-wrap'),handle=document.querySelector('.handle');function setBA(v){before.style.right=(100-v)+'%';handle.style.left=v+'%'}range?.addEventListener('input',e=>setBA(+e.target.value));setBA(50);
const form=document.getElementById('leadForm'),steps=[...document.querySelectorAll('.form-step')],stepText=document.getElementById('stepText'),bar=document.getElementById('progressBar');let current=0;function showStep(i){current=i;steps.forEach((s,n)=>s.classList.toggle('active',n===i));stepText.textContent=`Step ${i+1} of ${steps.length}`;bar.style.width=`${((i+1)/steps.length)*100}%`;form.scrollIntoView({behavior:'smooth',block:'center'})}function validStep(step){const fields=[...step.querySelectorAll('input,textarea,select')];for(const f of fields){if(!f.checkValidity()){f.reportValidity();return false}}return true}document.querySelectorAll('.next').forEach(b=>b.addEventListener('click',()=>{if(validStep(steps[current]))showStep(Math.min(current+1,steps.length-1))}));document.querySelectorAll('.back').forEach(b=>b.addEventListener('click',()=>showStep(Math.max(current-1,0))));
form?.addEventListener('submit',async e=>{e.preventDefault();if(!validStep(steps[current]))return;const submit=form.querySelector('.submit');submit.disabled=true;submit.textContent='Sending...';try{const r=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!r.ok)throw new Error();steps.forEach(s=>s.classList.remove('active'));document.querySelector('.progress').hidden=true;document.getElementById('success').hidden=false;form.reset()}catch{alert('The form could not send. Please call (941) 404-9777 or email axelsllc22@gmail.com.');submit.disabled=false;submit.textContent='Request My Free Estimate'}});
const lightbox=document.getElementById('lightbox'),lightImg=lightbox?.querySelector('img');document.querySelectorAll('[data-photo]').forEach(btn=>btn.addEventListener('click',()=>{lightImg.src=btn.dataset.photo;lightbox.hidden=false}));lightbox?.querySelector('button').addEventListener('click',()=>lightbox.hidden=true);lightbox?.addEventListener('click',e=>{if(e.target===lightbox)lightbox.hidden=true});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&lightbox)lightbox.hidden=true});document.getElementById('year').textContent=new Date().getFullYear();
