const menu=document.querySelector('.menu'),header=document.querySelector('.site-header');menu?.addEventListener('click',()=>header.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>header.classList.remove('open')));
const range=document.querySelector('.compare input'),before=document.querySelector('.before'),compare=document.querySelector('.compare');range?.addEventListener('input',e=>{const v=e.target.value;before.style.width=v+'%';compare.style.setProperty('--split',v+'%');compare.style.setProperty('');compare.style.cssText+=`;`;compare.querySelector(':scope')});
if(range){range.addEventListener('input',()=>{compare.style.setProperty('--x',range.value+'%')});const style=document.createElement('style');style.textContent='.compare:after{left:var(--x,50%)}';document.head.appendChild(style)}
const form=document.getElementById('leadForm'),steps=[...document.querySelectorAll('.form-step')],next=document.getElementById('nextBtn'),back=document.getElementById('backBtn'),submit=document.getElementById('submitBtn'),bar=document.getElementById('progressBar'),label=document.getElementById('stepLabel');let current=0;
function render(){steps.forEach((s,i)=>s.classList.toggle('active',i===current));bar.style.width=((current+1)/steps.length*100)+'%';label.textContent=`Step ${current+1} of ${steps.length}`;back.style.display=current?'inline-flex':'none';next.style.display=current===steps.length-1?'none':'inline-flex';submit.style.display=current===steps.length-1?'inline-flex':'none'}
function valid(){const fields=[...steps[current].querySelectorAll('input,textarea')];for(const f of fields){if(!f.checkValidity()){f.reportValidity();return false}}if(current===0&&!steps[0].querySelector('input[name="service"]:checked')){alert('Please choose a service.');return false}return true}
next.addEventListener('click',()=>{if(valid()){current++;render()}});back.addEventListener('click',()=>{current--;render()});
form.addEventListener('submit',async e=>{e.preventDefault();if(!valid())return;submit.disabled=true;submit.textContent='Sending...';try{const res=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});if(!res.ok)throw new Error();form.classList.add('completed')}catch(err){alert('There was a problem sending the form. Please call (941) 404-9777.');submit.disabled=false;submit.textContent='Request Free Estimate'}});render();
